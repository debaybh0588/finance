PORT=4000
NODE_ENV=development

# Postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=accounting_ai
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SSL=false

# Tenant and branch defaults
DEFAULT_TENANT_ID=tenant_demo
DEFAULT_BRANCH_ID=branch_main

# Future integrations (not used yet)
N8N_BASE_URL=
# Optional fallback workflow key used for n8n webhook auth (header: x-workflow-key)
N8N_API_KEY=
TALLY_BASE_URL=
TALLY_API_KEY=
